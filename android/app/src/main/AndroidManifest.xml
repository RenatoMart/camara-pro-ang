<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Cámara (VisionCamera) y lectura del carrete para la galería.
         READ_MEDIA_IMAGES aplica desde Android 13; el permiso antiguo
         queda limitado a versiones anteriores. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Audio del modo vídeo. Se pide sólo al entrar a ese modo
         (`useMicrophonePermission`), no al arrancar la app: sin él se graba
         igual, pero mudo. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <!-- Vertical fijo, sin excepción.
           `user` (lo que había antes) no es "no girar": sigue
           `Settings.System.USER_ROTATION`, la última orientación que quedó
           marcada a nivel de sistema con la rotación automática desactivada
           — y ese valor puede ser horizontal (lo dejó así otra app, o el
           propio HyperOS/MIUI) sin que el usuario lo haya pedido en esta.
           `portrait` bloquea del todo: la app nunca gira, pase lo que pase
           con el sensor o con el ajuste del sistema — es justo lo que se
           quiere en una app de cámara y vídeo, donde grabar a media rotación
           no es cosmética, es un archivo con la orientación mal. -->
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
